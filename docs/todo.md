# TODO

- Add an IO event adapter that maps slot/channel/value inputs to motion controls.
- Add indexed color palette output for LED matrix targets.
- Add GIF/APNG or raw frame streaming helper after the PPM frame generator is stable.
- Add benchmark mode for high frame-count generation.
- Define joint-frame JSON schemas for IO samples, predicted joints, and scenario metrics.
- Replace primitive Three.js runner with a named multi-joint rig.
- Add 300ms IO replay and 16ms/33ms prediction timeline visualization.
- Add deterministic gait baseline, interpolation fallback, and residual learner.
